# Guest exports

Learning sessions recorded under the automatic Guest account use the logical path `data/guest/sessions/<session-id>.json`.

Runtime captures remain in browser IndexedDB. Exported JSON is ignored here by default to avoid committing private tracking data.
